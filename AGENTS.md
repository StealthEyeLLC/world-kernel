# Build 001 agent instructions

The two files under `docs/preregistration/original/` are immutable historical artifacts. Never edit, normalize, or replace them.

Authority order:

1. Machine-readable preregistration for locked fields represented there.
2. Human-readable Build 001 specification for remaining architecture and experiment semantics.
3. Canonical Eye-line documents for any sibling surface touched.
4. Live provider state for implementation mechanics only.

Do not add universal Entity, State, Event, Rule, Hypothesis, Capability, Plan, Experiment, Skill, embedding, graph-node, or graph-edge primitives. Do not modify Eye, CODEeye, or eyeBROWSE. Do not create a Windows service, background agent, planner, workflow engine, vector store, or graph database. Use only the disposable fixture repository for destructive provider tests.

Confirmatory data is locked after the first confirmatory block begins. Never tune metrics, arms, scoring, exclusions, proposition vectors, thresholds, or gates after that boundary.

