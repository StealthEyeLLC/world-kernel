# World Kernel Build 001 — measured results

Execution date: 2026-08-11  
Final classification: **NO VALID CONFIRMATORY CONCLUSION**  
Canonicalization: **not authorized and not performed**

## 1. Executive result

The complete bounded candidate slice was implemented: normalized PostgreSQL schema, immutable content-addressed Evidence, bitemporal Claims, conservative repository correspondence, pre-dispatch Predictions, semantic ActionAttempts, fresh post-action Outcomes, PredictionEvaluations, TransitionEpisodes, deterministic Memory/Treatment serializers, the locked Brier scorer, the paired bootstrap/randomization implementation, live CODEeye and eyeBROWSE adapters, an experiment-owned native Git facet, deterministic fixture resets, recovery handling, blinded evaluator separation, and preflight/hostile/overhead harnesses.

The central comparative experiment was **not validly executable**. Preflight P0 failed because the exact model deployment identifier and configuration were not exposed; P2 failed because the eyeBROWSE GitHub profile was anonymous and could not perform the required browser-mediated remote commit; P5 failed because no supported mechanism available to this run could attest genuinely fresh, isolated model invocations with fixed model/configuration and no cross-arm memory. Per the frozen specification, the harness refused acquisition, pilot, confirmatory, and drift phases. It did not simulate fresh sessions or generate post-hoc trial data.

Accordingly, the Treatment-versus-Conventional-Memory hypothesis is neither supported nor falsified. Build 002 is not justified.

## 2. Final classification

**NO VALID CONFIRMATORY CONCLUSION** (`NO_CONFIRMATORY_CONCLUSION` in the machine-readable allowed-conclusion vocabulary).

This is not PASS, PARTIAL PASS, ARCHITECTURE REDUCTION, or architecture FAILURE. The observed blocker is the inability to satisfy mandatory experimental preconditions, not a measured loss or win by either arm. The implementation evidence remains useful, but it cannot answer the preregistered primary question.

## 3. Governing file hashes

| Immutable received file | Expected SHA-256 | Received SHA-256 | Result |
|---|---|---|---|
| `StealthEye_World_Kernel_Build_001_Spec_and_Preregistration.md` | `63804abcc376c4e2c27f242bee16e10678458b1befd5d1e4903ff54fa2d87696` | `63804abcc376c4e2c27f242bee16e10678458b1befd5d1e4903ff54fa2d87696` | exact |
| `StealthEye_World_Kernel_Build_001_Preregistration.json` | `cf4de0ea97cd394ec8ae9373617d253b9b220be11a6584d30380b0051a617b10` | `cf4de0ea97cd394ec8ae9373617d253b9b220be11a6584d30380b0051a617b10` | exact |

The byte-identical originals were frozen in Git commit `22f9f8e459d71f8df271078575c920db9c6469b2`. The evaluator freeze state is `original_frozen`. No original was rewritten.

## 4. Repository and machine baselines

| Item | Measured baseline |
|---|---|
| Machine / OS | `STEALTHEYELLC`; Microsoft Windows 11 Home `10.0.26200` (build 26200), x64 |
| Interactive user | `STEALTHEYELLC\StealthEye` |
| Eye service | live as LocalSystem; existing sole permanent Eye SCM service |
| Git | `C:\Program Files\Git\cmd\git.exe`, `2.55.0.windows.3` |
| .NET SDK | `10.0.302` |
| Node | `24.18.1` from the existing eyeBROWSE toolchain |
| Chrome | `151.0.7922.109` |
| GitHub namespace | `StealthEyeLLC`, provider type `User`, provider ID `247854506` |
| World Kernel repository | `StealthEyeLLC/world-kernel`, provider ID `1330898472` |
| Fixture repository | `StealthEyeLLC/world-kernel-build-001-fixture`, provider ID `1330898503` |
| Experiment root | `X:\WorldKernel\Build001` |
| Disk at baseline | X: 322,122,547,200 bytes total / 316,866,809,856 free; C: 700,747,608,064 total / 604,043,415,552 free |

The unrelated dirty local Eye branch at `X:\Repos\eye` (`build/phase-7-blackboard-relay`, local `92e4a9af...`) was preserved and never modified.

## 5. Exact model, version, and configuration

| Field | Recorded value |
|---|---|
| Product surface | ChatGPT Work / Codex |
| Reported family | GPT-5 class |
| Exact deployment identifier | **unavailable** |
| Configuration hash | **unavailable** |
| Confirmatory eligibility | false |
| Implementation invocation | one ongoing implementation conversation; excluded from any arm statistic |

No exact identifier or configuration was invented. This missing information is P0 failure evidence and independently prevents the same-model/configuration assertion required by P5 and D6.

## 6. Exact CODEeye HEAD

`StealthEyeLLC/CODEeye` default-branch HEAD used: `1ca0f93d64bc20bccb3b96dbcda43a2232783609`.

The live `\\.\pipe\codeeye-dev` Program Host was probed against the isolated fixture worktree. `repo.status` and `world.sync` reflected the exact controlled local change. CODEeye was not modified.

## 7. Exact eyeBROWSE HEAD

`StealthEyeLLC/eyebrowse` default-branch HEAD used: `2e27f44ebd3522d0d26b036dc57f790535df3533`.

The live `\\.\pipe\eyebrowse-dev` Program Host exposed browser incarnation, target/document state, observation cursors, DOM/accessibility state, Chrome, and CDP runtime. Browser-native read and stale-versus-fresh observation succeeded; authenticated GitHub write did not.

## 8. Exact Eye HEAD

`StealthEyeLLC/eye` default-branch HEAD used: `53948b74701f51c29c9322dfa9f017ba6b45f4a4`.

The active machine contract was the live v1 inspect/run surface. The repository's v2 contract is a target contract and was not falsely reported as the running surface. Eye was not modified.

## 9. World Kernel implementation commit

| Milestone | Commit |
|---|---|
| Frozen governing originals | `22f9f8e459d71f8df271078575c920db9c6469b2` |
| Core implementation | `8685ba5eec7e6ec5e725453d280dea0f24d101d0` |
| Completed preflight, hostile, recovery, and measurement harness | `3250c631acd51a787eeab9a1af6a3c6e93228f97` |

`3250c631...` has tree `183c4a6e729f40e44356bf0140c562a462dca21d`. The final publication commit is the `main` commit containing this result document and `artifacts/ARTIFACT-INDEX.json`; its exact SHA is recorded in the final handoff because a Git commit cannot truthfully contain its own SHA.

## 10. Fixture repository commit and configuration

The fixture main seed/configuration commit is `519d05879314cab45280a9f58efbd8859ecd8d64`. `main` was protected against force push/deletion and required the deterministic successful fixture check. The fixture defines only disposable branches, deterministic `fixture/state.txt`, accepted/rejected push regimes, success/failure/no-check regimes, and fresh/stale browser regimes.

Real provider hostiles used disposable branch `wk-b001-hostile-connector`: base `519d0587...`, local unpushed commit `a5f00bee...`, evaluator remote mutation `bb63056a...`, and later stale-page mutation `a2d6f116...`. No production or sibling repository was used as an experiment target.

## 11. PostgreSQL version and runtime topology

PostgreSQL `18.4` was provisioned from the EDB Windows x64 binary archive whose SHA-256 is `02e239529ed7833d169f98d915d3feffe0813264b08b3ae353e78e8b9c97e1a6`. It ran on demand at loopback-only `127.0.0.1:55431`, with SCRAM authentication, experiment-owned binary/data/secrets paths, and a triggerless scheduled-task launcher. No PostgreSQL or World Kernel Windows SCM service was installed.

Crash recovery preserved exact counts `(Evidence, Observation, ActionAttempt, TransitionEpisode) = (48, 24, 20, 4)`. Recovery marked 12 interrupted actions and created no fictitious episode or process continuity. At final shutdown `pg_isready` returned exit 2 / `no response`; zero matching SCM services existed. The on-demand task remains Ready, not running.

## 12. Pilot result

**Not started.** The preregistered 12-trial pilot was prohibited because P0-P6 did not all pass. Pilot rows: 0. Pilot results were not used anywhere.

## 13. Preregistration amendments

None. The original remains v1 and immutable. No confirmatory seed or outcome was exposed, and `first_confirmatory_block_started = false`.

## 14. Acquisition corpus summary

**Not started.** Source acquisition blocks: 0; eligible acquired episodes: 0. Therefore no empirical Memory or Structured acquisition package exists. Serializer behavior and parity were validated only on four explicitly synthetic implementation-benchmark episodes; those data are not experimental evidence.

## 15. Confirmatory corpus summary

**Not started.** Independent matched configuration blocks: 0. Cold runs: 0; Conventional Memory runs: 0; Structured Treatment runs: 0. Confirmatory Predictions, Outcomes, Evaluations, and Episodes: 0. Missing/censored confirmatory blocks: 0 because none began. Preregistered exclusions: 0.

## 16. Primary Brier result

| Quantity | Result |
|---|---|
| Memory mean block-level Brier loss | not estimable |
| Structured mean block-level Brier loss | not estimable |
| Treatment relative reduction | not estimable |
| Independent matched blocks | 0 |

No probabilities were fabricated and no proposition was silently dropped.

## 17. Confidence interval and statistical test

The paired 10,000-resample block bootstrap, its 95% interval, and the paired label-randomization test are implemented and unit-tested, but no confirmatory input exists. Confidence interval: `null`. Randomization-test p-value: `null`. The preregistered 20% effect target remains unchanged.

## 18. Cold comparison

Not estimable. Cold Brier loss, completion, excess actions, and calibration metrics are all `null` because no Cold run occurred.

## 19. Conventional Memory result

Not estimable. The conventional serializer passed deterministic, bounded, same-source lineage tests and produced 6,322 bytes in the synthetic four-episode benchmark. That is implementation evidence, not arm performance.

## 20. Structured Treatment result

Not estimable. The structured serializer passed deterministic, bounded, same-source lineage tests and produced 6,832 bytes from the same four synthetic episodes with zero extra LLM calls. That is implementation evidence, not arm performance.

## 21. Behavioral value

Treatment-versus-Memory excess material actions, task completion, recovery, failed actions, consequential errors, provider calls, model calls, and token use are not estimable. No arm took a material action. The behavioral co-gate is therefore INCONCLUSIVE; improved code-level scoring cannot substitute for measured behavior.

## 22. Overhead

These are implementation/preflight measurements, not confirmatory arm overhead:

| Operation (20 samples unless noted) | Mean ms | p50 ms | p95 ms |
|---|---:|---:|---:|
| Content-addressed blob write | 1.614 | 0.563 | 1.757 |
| PostgreSQL Evidence insert | 3.899 | 0.337 | 4.084 |
| PostgreSQL exact Evidence read | 0.563 | 0.152 | 0.701 |
| Verified blob read | 0.166 | 0.085 | 0.227 |
| Deterministic Memory + Structured package pair | 2.577 | 0.260 | 2.690 |
| Memory serialization | 0.524 | 0.048 | 0.538 |
| Structured serialization | 1.597 | 0.175 | 1.672 |

Provider wall times were recorded separately: CODEeye observation 2,021 ms; eyeBROWSE observation 57,004 ms; local commit 116 ms; accepted push 1,700 ms; fetch 508 ms; fast-forward integration 61 ms.

Final measured storage was 10,712,767 bytes for kernel PostgreSQL, 8,804,031 bytes for evaluator PostgreSQL, 2,520 bytes of benchmark Evidence blobs, and 129,947 bytes of committed/uncommitted measurement artifacts at benchmark time. Experimental model calls/tokens were 0. Exact token telemetry for the implementation conversation was unavailable and excluded.

## 23. Correspondence suite

The implementation has no repository-level `same_as`. Local and hosted repositories remain separate Manifestations; `git:working_copy_of` requires provider ID, configured remote, exact compatible history, and independent evidence groups.

Across sufficiently evidenced hostile cases, false active hard correspondence count was 0, precision was 1.0, and eligible recall was 1.0. Same-name, same-content, changed-remote, path-reuse, divergence, and correlated-evidence tests passed. A hosted fork under a second owner and a live provider rename/redirect were unavailable; they remain explicitly ambiguous and excluded from the recall denominator. Gate B1 passes; the broader locator/identity-continuity Gate B2 is INCONCLUSIVE.

## 24. Temporal suite

Gate A2 passes for the implemented deterministic suite. Independent `valid_as_of` and `known_as_of` queries, delayed and out-of-order ingestion, stale presentation, historical correction, locator validity, replayed evidence, provider outage, branch deletion, and kernel-assigned record time returned the locked expected behavior. Projection rebuild was identical before/after: 4 claims, 2 derived beliefs, 14 Evidence records, and 2 episodes in the controlled rebuild fixture. No authoritative current-state table exists.

## 25. Evidence and epistemic integrity suite

Gate A3 passes. Evidence is immutable and content-addressed. Deliberate Prediction-as-Observation, inference-as-provider-fact, replay-as-fresh, receipt-as-Outcome, and backdated-record-time attempts were rejected or retained under their truthful type. Provider receipts cannot close verified Outcomes without fresh postcondition Observation. Append-only database triggers protect epistemic history.

The full implementation test suite passed 11/11 with 0 failures; result SHA-256: `d02c8bfcb9209b489ca5e93d3af870595120f8dea89d8399e5acfc6cef2fb4ad`.

## 26. Drift and belief-death result

**Not executed and INCONCLUSIVE.** The phase requires successive genuinely fresh cognitive invocations. Historical records were not rewritten, no persistent Rule object was added, and no synthetic contradiction sequence was presented as a measured drift result.

## 27. Fresh-instance proof

P5 failed. The product-native cloud browser reached a ChatGPT sign-in wall; the current conversation is the implementation context and cannot serve as a clean arm; no other supported route exposed an attestation for exact isolated model/configuration, empty prior transcript, disabled cross-chat memory, zero hidden evaluator state, and machine-readable pre-dispatch output. Owner credentials were not requested in chat. The harness rejected every experimental phase with nonzero exit status.

## 28. Hostile-suite matrix

| Case | Status | Measured result |
|---|---|---|
| Same-basename local decoy | PASS | Equal content/name did not create correspondence. |
| Hosted fork/provider clone | INCONCLUSIVE | Local shared-history test passed; no authorized second-owner hosted fork. |
| GitHub rename/redirect | INCONCLUSIVE | Resolver test passed; no live disposable provider rename. |
| Changed local remote | PASS | Old relation was not carried to URL-only replacement. |
| Stale browser page | PASS | Old target stayed stale; fresh eyeBROWSE target observed exact new SHA. |
| Local unpushed commit | PASS | Local advanced while remote stayed fixed. |
| Unseen remote commit | PASS | Remote advanced while local stayed fixed. |
| Same branch / different history | PASS | Exact ancestry preserved divergence. |
| Deleted test branch | PASS | Current existence ended; prior durable records remained. |
| Out-of-order observation | PASS | Both time axes returned expected answers. |
| Delayed provider check | PASS | Unresolved remained pending/censored. |
| Provider outage | PASS | Current truth became unknown, not assumed unchanged. |
| Replayed old Evidence | PASS | Original time/identity remained; no freshness laundering. |
| Prediction injected as Observation | PASS | Typed boundary rejected it. |
| Model inference as provider fact | PASS | Remained model-derived only. |
| Receipt success / absent postcondition | PASS | Could not become verified Outcome. |
| Partial material application | PASS | Protected-push rejection retained actual local/remote partial state. |
| Local path deleted/recreated | PASS | New Git incarnation remained distinct. |
| Identical-content independent clone | PASS | Content equality did not merge Manifestations. |
| Out-of-band remote mutation | PASS | Recorded as evaluator/non-operator with ambiguous attribution. |
| Correlated sensor Evidence | PASS | Shared dependency did not multiply confidence. |
| Stale runtime descriptor | PASS | Live probes controlled liveness. |
| Parameters changed after Prediction | PASS | Dispatch seal rejected mismatch and repeat dispatch. |
| Timestamp/backdating attack | PASS | Persistence boundary assigned record time. |
| Cross-arm state leak | INCONCLUSIVE | DB ACL/package probes passed; invocation isolation unattested. |
| Policy reversal/belief death | INCONCLUSIVE | Correctly not run without P5. |
| Authenticated browser remote commit | INCONCLUSIVE | eyeBROWSE reads passed; anonymous profile could not write. |

Total: 27 cases; 22 PASS, 0 FAIL, 5 INCONCLUSIVE. Suite conclusion: **INCONCLUSIVE**. No pilot or confirmatory data was used.

## 29. Acceptance gates

### Preflight gates

| Gate | Status | Evidence-based reason |
|---|---|---|
| P0 Baseline capture | FAIL | Machine/provider/commit/runtime baselines captured; exact model deployment/config unavailable. |
| P1 Live CODEeye | PASS | Live named-pipe status and controlled `world.sync` succeeded. |
| P2 Live eyeBROWSE | FAIL | Read/stale observation live; authenticated browser remote commit absent. |
| P3 Experimental Git facet | PASS | Observe, branch, commit, accepted/rejected push, fetch, ff-only integration, receipts, and CODEeye reobservation succeeded. |
| P4 PostgreSQL runtime | PASS | On-demand 18.4, migrations, tests, crash recovery, restart durability, loopback binding, and projection rebuild succeeded. |
| P5 Fresh invocation | FAIL | No supported attested isolated model context. |
| P6 Deterministic reset | PASS | Repeated seeds reproduced exact refs, bytes, policy/check regime, and differing hidden fingerprints. |

### Preregistered acceptance gates

| Gate | Status | Scope/result |
|---|---|---|
| A1 Store/replay integrity | PASS | Required records, durable restart, identical projection rebuild, no authoritative current table. |
| A2 Bitemporal correctness | PASS | 100% of implemented deterministic temporal hostiles returned expected answers. |
| A3 Epistemic type integrity | PASS | 100% of deliberate laundering attempts were rejected or truthfully typed. |
| B1 Correspondence precision | PASS | 0 false active relations; precision 1.0; eligible recall 1.0; ambiguity preserved. |
| B2 Locator/identity continuity | INCONCLUSIVE | Local/real provider cases passed; live hosted fork and provider rename were unavailable. |
| C1 Complete measured episodes | PASS | Four controlled implementation/preflight episodes closed completely; no confirmatory episode was analyzed. |
| C2 Prediction ordering | PASS | Transactional durable-prediction-before-dispatch constraint passed all analyzed implementation actions. |
| C3 Receipt/outcome separation | PASS | Receipt-only and absent-postcondition cases remained unverified. |
| C4 Fixture isolation | PASS | Zero material mutations outside designated Build 001 resources. |
| D1 Primary predictive advantage | INCONCLUSIVE | No matched blocks; effect, CI, p-value, and pilot-derived N absent. |
| D2 Behavioral value | INCONCLUSIVE | No arm behavior occurred. |
| D3 Fresh-instance inheritance | FAIL | Complete conversational reset was not attestable. |
| D4 Contradiction recovery | INCONCLUSIVE | Drift not authorized. |
| D5 More-than-memory | INCONCLUSIVE | Structured versus Memory was not measured. |
| D6 Compute/information parity | FAIL | Serializer/source parity tests passed, but exact same model/config and fresh arm schedule could not be established. |

Requested integrity summary: temporal **PASS**; evidence/epistemic **PASS**; prediction ordering **PASS**; safety/fixture targeting **PASS**; correspondence identity **INCONCLUSIVE** with zero observed false hard merges; fresh-instance isolation **FAIL**.

## 30. Unexpected findings

- The `StealthEyeLLC` namespace is provider-typed as a GitHub User rather than an Organization.
- The machine Git credential path created and initially configured the two authorized repositories, then became unavailable under the SYSTEM runtime. Publication continued through the already-authorized GitHub connector; no credentials were extracted or printed.
- eyeBROWSE could perform exact live browser/provider reads and demonstrate stale-target semantics, but its Chrome profile was anonymous.
- The available cloud browser was separately signed out of ChatGPT, so it did not provide a fresh-session execution route.
- A real GitHub protected-branch `GH006` rejection supplied useful receipt/postcondition evidence without changing sibling repositories.

## 31. Architecture consequences

Build 001 now supports a narrow claim: the frozen relational/epistemic mechanics can be implemented with PostgreSQL 18, preserve append-only bitemporal history across crash/restart, enforce pre-dispatch predictions, avoid the tested false-identity merges, integrate with the existing Eyes without changing them, and derive bounded same-source Memory/Structured packages without extra model calls.

It does **not** support a claim that structured inheritance improves prediction or behavior over conventional memory. It also does not falsify that comparative hypothesis. No general world model, causal model, continuous consciousness, model-weight learning, or canonical StealthEye architecture has been established.

## 32. What must explicitly not be built next

Do not start Build 002; canonicalize a World Kernel; rename it `WORLDeye`; add a planner, Rule object, generic ontology, capability graph, workflow DSL, autonomous agent, vector memory, graph database, permanent service, or CODEeye/eyeBROWSE API expansion on this evidence. The unresolved prerequisite is experimental execution capability, not a need for a larger architecture.

## 33. Reproduction instructions

On `STEALTHEYELLC`, from `X:\WorldKernel\Build001\repo`:

```powershell
./scripts/start-postgres.ps1
./scripts/test.ps1
./scripts/recovery-test.ps1
./scripts/finalize-preflight.ps1
./scripts/finalize-hostile-suite.ps1
dotnet run --project ./src/WorldKernel.Build001 -- overhead-measure --repo-root . --secret-file C:\WorldKernel\Build001\runtime\secrets\connections.json --store-root X:\WorldKernel\Build001\evidence\blobs --output ./artifacts/overhead/implementation-overhead.json
./scripts/run-experiment.ps1 -Phase acquisition
./scripts/stop-postgres.ps1
```

The phase command must refuse while any P0-P6 gate fails. Do not bypass it. The exact PostgreSQL archive URL/hash, data path, port, seed manifest, evaluation spec, scorer/serializer versions, evidence hashes, provider traces, and phase-refusal proof are persisted under `artifacts/`, `experiments/`, `schemas/`, and `docs/preregistration/`.

## 34. Artifact and hash index

The complete generated index is `artifacts/ARTIFACT-INDEX.json`. Key immutable hashes include:

| Artifact | SHA-256 |
|---|---|
| Human governing specification | `63804abcc376c4e2c27f242bee16e10678458b1befd5d1e4903ff54fa2d87696` |
| Machine preregistration | `cf4de0ea97cd394ec8ae9373617d253b9b220be11a6584d30380b0051a617b10` |
| Evaluation spec | `f182f6b0ac91d85436f077f0c78e3db0ec3b35d2f15f82d0675b699598c93ded` |
| Final implementation tests | `d02c8bfcb9209b489ca5e93d3af870595120f8dea89d8399e5acfc6cef2fb4ad` |
| Final preflight gates | `5f4195e5b8d8ee9047d78cc16f3d3dc5459e562e14e3e76d732cdfbda6e48e00` |
| P5 fresh-invocation blocker | `ca3a8547543cd6489e62dac5e963ef7415f0105b5c8eed6379950335611676c7` |
| Hostile-suite matrix | `e593c89e5bfb6a5d79b04511e1e7a5b99a9ccef81bdfcafc0fe5082a48bee6f2` |
| Overhead measurements | `57fe8606ee2a3c49d3d3a54f07c9798c7cf44d7b296d2c6205caabcf83f6b723` |

Raw provider Evidence is either committed directly or represented by immutable content-addressed references with digest, length, media type, observer/provider, capture method/time, and stable blob location. No secrets are committed.

## 35. Whether Build 002 is justified

**No.** The smallest evidence-justified next action is not a new architecture build. It is a future rerun of Build 001 preflight after an authorized, attestable fresh-invocation mechanism and an authenticated disposable eyeBROWSE GitHub profile exist, with exact model/configuration exposed. Only if all P0-P6 gates then pass should the existing unchanged harness proceed to acquisition, the 12-trial pilot, a frozen pilot-derived sample size, confirmatory execution, and drift.
