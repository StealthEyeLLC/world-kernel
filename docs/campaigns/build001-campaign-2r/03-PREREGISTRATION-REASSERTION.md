# Campaign 2R — Preregistration Reassertion

Date: 2026-08-11
Campaign: `build001-campaign-2r`
Status: prospective, pre-acquisition

## Authority chain

This document does not replace or pretend to be the original preregistration. It reasserts the unchanged Build 001 design after Campaign 2 / Run A's immutable execution-integrity FAILURE.

Governing predecessors remain:

1. the original Build 001 specification/preregistration;
2. the Campaign 2 preregistration amendment;
3. Campaign 2 / Run A terminal FAILURE at `4bc35bac9ee20268f3ce2966fc62f2c2ed4cff0b`;
4. Campaign 2R authority at the first Run B commit.

## What changed

Only execution implementation required to obey the already-existing provider/subject Claim-lineage architecture, plus equivalent mechanical enforcement defects discovered by the complete Claim-path audit, changed.

The exact execution-file old/new SHA-256 inventory is `artifacts/campaign-2r/changed-execution-files.json`.

The decisive change is that hosted `github:remote_ref_head` truth is now grounded in an exact GitHub-provider observation (`git ls-remote` retained command/result) targeted at the hosted Manifestation, rather than local working-copy evidence. Local and hosted pre/post Observations remain separate. Correspondence relates them without evidence relabeling. Recovery and public EpisodeExport preserve the same separation.

The existing lineage gate was strengthened to mechanically enforce provider-family compatibility, successful Observation freshness for material/provider Claims, and receipt exclusion as primary verified Claim Evidence. No hard gate was weakened and no new ontology primitive was added.

Run B storage/campaign IDs and paths were separated from Run A. No Run A subject output, seed, lifecycle row, or result is reusable as Run B scientific data.

## What did not change

The following are frozen unchanged:

- Build 001 hypothesis;
- Cold / Conventional Memory / Structured arms;
- primary Structured-versus-Memory contrast;
- configuration-block mean Brier primary endpoint;
- 20% effect target;
- 10,000 paired bootstrap resamples and 95% interval;
- locked paired randomization test;
- the six semantic action classes;
- Memory headroom rule;
- 12-block pilot;
- sample-size formula, including `N > 96 -> redesign/re-pilot` and no clipping;
- behavioral gate;
- drift gate;
- identity, temporal, and epistemic gates;
- fresh-instance requirement;
- equal-information requirement;
- equal-compute/model-call requirement;
- fixture regime family and difficulty;
- protected rejection/acceptance balances;
- reset/fingerprint requirement;
- workflow attribution rule;
- retry/idempotency rule;
- P0 observable-product-equality boundary;
- hosted browser-mediated subject action through eyeBROWSE;
- ephemeral GitHub authentication and secret hygiene.

The subject target remains ChatGPT web / 5.6 Sol / Extra High / brand-new Temporary Chat. Because Run B path/adapter bytes changed, P0/P5 will prospectively re-attest the current observable product/driver contract before acquisition; this is mechanical re-attestation, not a subject-information change.

## Prospective boundary

No Campaign 2R scientific subject has been invoked. No Campaign 2R acquisition seed has opened. The non-scientific lifecycle/six-action audit used an isolated harness database and a disposable fixture workspace, and never dispatched a material action.

A separate prospective execution freeze will be created only after fresh P0-P6 all PASS and Campaign 2R scientific counters are independently verified zero.

Once the first Campaign 2R scientific subject invocation occurs under that freeze, execution code is frozen again. A later frozen implementation defect must be preserved and adjudicated rather than patched through.

Build 002 remains unauthorized.